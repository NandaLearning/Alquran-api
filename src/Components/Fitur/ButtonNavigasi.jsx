const ButtonNavigasi = (props) => {
  return (
    //eslint-disable-next-line
    <button className={`${props.bg} rounded-2xl w-24 ${props.color}`}>{props.text}</button>
  )
}

export default ButtonNavigasi
